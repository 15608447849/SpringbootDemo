<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false">
    <!--定义日志文件的存储地址 -->
    <property name="LOG_HOME" value="./logs" />

    <!-- scope="context" 表示全局可用，defaultValue为默认值 -->
    <springProperty name="SERVICE_NAME" scope="context"  source="spring.application.name" defaultValue="unknown"/>

    <!-- 按照每天生成日志文件 -->
    <appender name="FILE"
              class="ch.qos.logback.core.rolling.RollingFileAppender">
        <encoder
                class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符 -->
<!--            <pattern>[%X{requestId}] %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{50}:%line - %msg%n</pattern>-->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{50}:%line - %msg%n</pattern>
        </encoder>
        <rollingPolicy
                class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!--日志文件输出的文件名 -->
            <FileNamePattern>${LOG_HOME}/${SERVICE_NAME}/%d{yyyy-MM-dd}.log</FileNamePattern>
            <!--日志文件保留天数 -->
            <maxHistory>7</maxHistory>
            <cleanHistoryOnStart>true</cleanHistoryOnStart>
        </rollingPolicy>
    </appender>

    <!-- 控制台输出 -->
    <appender name="STDOUT"
              class="ch.qos.logback.core.ConsoleAppender">
        <encoder
                class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符 -->
<!--            <pattern>[%X{requestId}] %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level %logger{50}:%line - %msg %n</pattern>-->
            <pattern>- %d{yyyy-MM-dd HH:mm:ss.SSS} %-5level - %msg %n</pattern>
        </encoder>
    </appender>

    <!-- 屏蔽 日志 -->
    <logger name="org.apache.tomcat" level="WARN"/>
    <logger name="com.alibaba.nacos" level="INFO"/>
    <logger name="com.alibaba.cloud.nacos" level="INFO"/>
    <logger name="io.grpc" level="WARN"/>
    <logger name="io.netty" level="WARN"/>
    <logger name="com.alibaba" level="WARN"/>
    <logger name="org.springframework" level="WARN"/>
    <logger name="org.springframework.boot" level="WARN"/>
    <logger name="org.springframework.cache" level="WARN"/>
    <logger name="org.hibernate" level="WARN"/>
    <logger name="io.lettuce.core" level="WARN"/>
    <logger name="org.mybatis.spring.mapper" level="WARN"/>
    <logger name="org.springdoc" level="WARN"/>
    <logger name="org.springdoc.webmvc" level="WARN"/>
    <logger name="org.apache.catalina" level="WARN"/>
    <logger name="io.swagger" level="WARN"/>
    <logger name="sun.management" level="WARN"/>
    <logger name="jdk.management" level="WARN"/>
    <logger name="jdk.internal" level="WARN"/>
    <logger name="com.zaxxer.hikari" level="ERROR"/>
    <logger name="org.apache.catalina" level="ERROR"/>

    <!-- 日志输出级别 -->
    <root level="DEBUG">
        <appender-ref ref="STDOUT" />
        <appender-ref ref="FILE" />
    </root>
</configuration>